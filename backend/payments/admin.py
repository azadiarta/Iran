# The payments app contains no Django models.
# Contribution (the payment record) is registered in fund/admin.py.
# Provider classes (ManualPaymentProvider, PayPalPaymentProvider, PaymentFactory)
# are pure Python — nothing to register here.
