{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd backend && python manage.py migrate --noinput && python manage.py seed_initial_data && python manage.py collectstatic --noinput && gunicorn groupfund.wsgi --bind 0.0.0.0:$PORT --workers 3 --log-file -",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
